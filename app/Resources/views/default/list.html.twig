{% extends 'base.html.twig' %}

{% block body %}
    <div id="wrapper">
        <div id="container">
            <div id="welcome">
                <ul>
                    {% for item in items %}
                        <li>
                            <a href="/item?name={{ item.name|e }}">{{ item.name|e }}</a>
                        </li>
                    {% endfor %}
                </ul>
            </div>

        </div>
    </div>
{% endblock %}

{% block stylesheets %}
{% endblock %}
